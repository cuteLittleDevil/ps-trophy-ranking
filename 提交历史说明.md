# PR #4 提交历史说明

## Git 提交记录

PR #4 已成功合并至 main 分支，包含以下功能提交：

### 1. 初始实现（a358d0b）
```
feat: implement PSN trophy leaderboard v1
```
- 完整实现 PSN 奖杯排行榜 v1
- 新增 2,595 行代码
- 包含全部核心功能、UI、测试

### 2. SPEC 对齐修复（c78c557 / e1d57ce）
```
修复：对齐 SPEC 架构要求
```

**主要变更**：
1. **分数计算统一** - `rank.Score` 作为公式唯一来源，删除 `psn/score.go`
2. **冷却时间改用数据库** - 基于 `players.synced_at` 判断，移除内存状态
3. **CLI 校验放宽** - 允许数字开头，对齐 SPEC §5.2
4. **新增错误类型** - `invalid_credentials` 独立消息
5. **补全配置示例** - `.env.example` 添加全部 4 个配置项

**技术细节**：
- `internal/rank/rank.go`: Score() 函数作为公式唯一实现
- `internal/trophy/source.go`: 移除 LastSync() 接口
- `cmd/psnlookup/main.go`: 去掉字母开头限制
- HTTP handler: 冷却检查改为查询 `store.Get(id).SyncedAt`

### 3. 产品需求跟进（f0465d3 / 2f4bbdf）
```
修复：实现产品需求跟进修正（PR #4）
```

**本次变更响应产品团队最新决策**：

1. **搜索成功设置 Cookie**（产品决策覆盖）
   - GET /search 找到玩家后设置 `online_id` cookie
   - 属性：Path=/、HttpOnly、SameSite=Lax、Max-Age=2592000
   - 支持 US-007「我的排名」功能
   - 测试：`TestSearchFound`

2. **头像 URL 安全校验**（评审安全必修项）
   - 仅接受 `https://` 或空字符串
   - 拒绝 `http://`、`javascript:`、`data:`、`ftp:` 等
   - 测试：`TestJoinValidatesAvatarURL` 覆盖 5 种协议

3. **负数奖杯计数拒绝**（额外防御）
   - 检测负数时拒绝写入数据库
   - 测试：`TestJoinRejectsNegativeCounts`

**代码位置**：
- `internal/http/server.go:274-282` - 搜索设置 cookie
- `internal/http/server.go:211-215` - 头像 URL 校验
- `internal/http/server.go:206-209` - 负数拦截

### 4. 文档补充（b8c6041 / 7cc9842）
```
文档：补充 SPEC 对齐与产品需求验证文档
```

**新增文档**：
- `SPEC_ALIGNMENT_VERIFICATION.md` - SPEC 架构对齐验证清单
- `FINAL_VERIFICATION.md` - 产品需求最终验证检查表

**更新文档**：
- `README.md` - 新增 SQLite 纯 Go 实现说明
- `ROADMAP.md` - 标记 v1 完成

## 验证状态

### 测试结果
```bash
✅ go test ./...   # 全部 7 个包通过
✅ go build ./...  # 构建成功
```

### 功能覆盖
- ✅ PRD 全部 FR-1 至 FR-21
- ✅ SPEC 全部架构要求
- ✅ 全部 7 个用户故事（US-001 至 US-007）
- ✅ 安全校验（头像 URL、负数计数）
- ✅ 存储支持的冷却（重启后保留）

## 分支状态

- **main**: 已合并 PR #4 全部变更
- **cursor/psn-leaderboard-v1-0986**: 保留中文 commit message 版本（内容相同）

## PR 信息

- **编号**: #4
- **标题**: 功能：实现 PSN 奖杯排行榜 v1
- **状态**: 已合并
- **链接**: https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/4
- **描述**: 已更新为中文

---

*生成时间: 2026-09-11*
