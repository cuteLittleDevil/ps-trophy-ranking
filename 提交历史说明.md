# V1 提交历史说明

V1 排行榜已合入 `main`（HEAD `572781a`，2026-09-11）。本文记录从 CLI 到页面、再到去掉 fixture / 灌数 / 刷新的完整提交链，以及高吞吐架构实现（Phase 1+2 + 排序优化）。

产品需求：`tasks/prd-psn-trophy-leaderboard.md`  
技术方案：`tasks/spec-psn-trophy-leaderboard.md`

---

## 阶段总览

| 阶段 | PR | 内容 |
|------|----|------|
| CLI 与现场核对 | #1–#3 | `psnlookup`、`--raw`、调用链注释与流程图 |
| 排行榜首版 | #4 | 中文榜页、SQLite、入榜/查找/我的排名、fixture + PSN |
| 体验与依赖 | #5–#7 | 顶栏布局、chi 路由、go.mod 直接依赖 |
| 去掉 fixture | #8 | `/join` 只走真实 PSN；`POST /admin/seed` 追加模拟用户 |
| 头像与刷新 | #9–#10 | Sony CDN http 升级、首字母回退、`POST /refresh` |
| 高吞吐架构文档与实现 | #11–#16 | Phase 1+2 + 排序优化（全量内存 + Top1000 + WAL + pprof + slog） |

---

## 阶段一：PSN 查询 CLI（PR #1–#3）

### PR #1 — 按 Online ID 拉取奖杯汇总

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/1

- 新增 `cmd/psnlookup` 与 `internal/psn`
- 凭证只从 `PSN_NPSSO` / `.env` 读取（`.env` 已 gitignore）
- httptest 覆盖不存在、隐私、无效凭证、`me/profiles` 400 后回退搜索

### PR #2 — `--raw` 打印完整 JSON

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/2（commit `7ee6d05`）

```
feat: psnlookup --raw 打印索尼 API 完整 JSON
```

- 打印搜索 / `trophySummary` 等完整响应
- 禁止 dump oauth `/token` 与 NPSSO

### PR #3 — resty、金样与流程图

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/3（commit `30f3293`）

```
feat: 用 resty 拉取完整奖杯 JSON 并补充调用流程图
```

- resty 收拢 PSN 请求；`Summary.TrophyJSON` 保存完整 body
- 金样 / live 测试；`docs/flowchart.html`

同期文档：`690123d` 新增 v1 PRD / SPEC。

---

## 阶段二：排行榜首版（PR #4）

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/4  
标题：功能：实现 PSN 奖杯排行榜 v1  
状态：已合并

当时默认 `PSN_MODE=fixture`，真实 PSN 为可选。后续 PR #8 已删除该模式。

### 1. 初始实现（`a358d0b`）

```
feat: implement PSN trophy leaderboard v1
```

- 中文榜页、积分/竞赛名次、SQLite、入榜/查找/我的排名
- fixture 演示数据与可选真实 PSN
- 新增约 2,595 行

### 2. SPEC 对齐修复（`c78c557`）

```
fix: align implementation with SPEC requirements
```

1. **分数计算统一** — `rank.Score` 作为公式唯一来源，删除 `psn/score.go`
2. **冷却改用数据库** — 基于 `players.synced_at`，移除内存 `lastSync`
3. **CLI 校验放宽** — 允许数字开头，对齐 SPEC §5.2
4. **新增错误类型** — `invalid_credentials` 独立中文消息
5. **补全配置示例** — `.env.example` 当时含 `PSN_MODE` 等 4 项（PR #8 已去掉 `PSN_MODE`）

### 3. 产品需求跟进（`f0465d3`）

```
fix: implement follow-up review fixes for PR #4
```

1. **搜索成功设置 Cookie** — GET `/search` 命中后设置 `online_id`（Path=/、HttpOnly、SameSite=Lax、Max-Age=2592000）
2. **头像 URL 安全校验** — 当时仅接受 `https://` 或空（PR #9 改为 Sony CDN `http://` 可升级）
3. **负数奖杯计数拒绝** — 负值不写库

### 4. 文档补充（`b8c6041`）

```
docs: add final verification checklist for all PR #4 requirements
```

- 当时附带的 PR #4 验收快照文档已删除；现行契约以 SPEC / PRD 为准

---

## 阶段三：体验与依赖（PR #5–#7）

### PR #5 — 顶栏控件重叠

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/5

| Commit | 说明 |
|--------|------|
| `2c3229a` | 文档：添加 PR #4 中文提交历史说明 |
| `71ed5b1` | 修复：顶栏控件在中等宽度屏幕下重叠（首次 1100px 断点） |
| `580ca4d` | 修复：提高断点至 1500px，解决 1440px 仍重叠 |

最终布局：`>1500px` 单行；`769–1500px` 入榜独占第一行，查找与「我的排名」第二行；`≤768px` 纵向堆叠。

### PR #6 — HTTP 路由改用 chi

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/6（commit `9ce1e0e`）

```
重构：HTTP 路由改用 go-chi/chi
```

- `http.NewServeMux()` → `chi.NewRouter()`
- `middleware.Recoverer`；方法路由 `r.Get` / `r.Post`
- 行为不变，Handler 仍兼容 `net/http`

### PR #7 — go.mod 直接依赖

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/7（commit `8c6dabd`）

```
修复：将 chi 与 sqlite 标记为直接依赖
```

- `github.com/go-chi/chi/v5`、`modernc.org/sqlite` 从 `// indirect` 提升为直接依赖
- Go 版本保持 `1.25.0`

---

## 阶段四：去掉 fixture，灌数接口（PR #8）

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/8  
标题：重构：去除 fixture，新增 /admin/seed 追加模拟用户

### 1. 架构重构（`137e104`）

```
重构：去除 fixture，新增 /admin/seed 追加模拟用户
```

- 删除 `internal/trophy/fixture.go` 与 `fixture_test.go`
- 去掉 `PSN_MODE`；`/join` 只走真实 PSN
- 新增 `POST /admin/seed`（追加模拟用户，压测/演示）
- 模板数据源固定为「PSN 真实档案」；`letter:` 前缀字母头像

### 2. 评审必改（`35e2ed5`）

```
修复：PR #8 评审要求的必改项
```

1. Online ID：`sim` + 7 位定宽数字（总长 10，保证 3–16）
2. `count` 硬上限 1000，超限 400
3. 只接受回环地址，非回环 403
4. 补齐 seed 自动化测试（成功 / 非法 count / 超上限 / 累加 / 非回环）
5. PRD US-005 / FR-13 改为灌数接口需求

---

## 阶段五：头像与刷新（PR #9–#10）

### PR #9 — 头像回退与 Sony CDN http

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/9

| Commit | 说明 |
|--------|------|
| `9342c78` | 删除损坏的 `placeholder.png`；HTTPS 加载失败 / 空 URL 用 DisplayID 首字母块 |
| `974871b` | 索尼 CDN `http://static-resource.np.community.playstation.net/...` 自动升级为 https；其它 http / 非 http(s) 仍拒绝 |

### PR #10 — 排行榜手动刷新

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/10  
标题：实现「入榜即更新」+ 排行榜每行手动刷新

| Commit | 说明 |
|--------|------|
| `3189c83` | 新增 `POST /refresh`；真实用户行显示「刷新」；`sim` 前缀不显示 |
| `55e2b48` | 刷新表单带 hidden `page`，成功后回到原页；补 CSS 与 SPEC 端点说明 |

刷新与 `/join` 共享 15 分钟 `synced_at` 冷却；必须已在榜；`Upsert` 保留 `joined_at`。

---

## 阶段六：高吞吐架构文档与实现（PR #11–#16）

### PR #11 — 文档：高吞吐设计与 Mermaid 流程图

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/11  
标题：文档：高吞吐写入演进架构设计（已批准，待实现）

#### 关键变更

```
文档：增补高吞吐写入演进架构设计（第13节）与 Mermaid 流程图
修正：SPEC 章节顺序，将第 13 节移至第 12 节之后
```

**增补内容**：
- SPEC 新增第 13 节：高吞吐写入演进架构（全量内存 + Top1000 + 双路径 + WAL 封段）
- README 新增两张 Mermaid 流程图：写入流程（热/冷路径）、查询排行榜流程
- 设计目标：支撑 1w/s 级推送、秒级可见、继续用 SQLite
- 方案对比：对比 6 种备选方案，选择全量内存 + Top1000 + 双路径 WAL
- 实现里程碑：Phase 1（内存）、Phase 2（WAL）、Phase 3（可观测）、Phase 4（热路径 micro-batch）

**文档位置**：
- `tasks/spec-psn-trophy-leaderboard.md` §13（+297 行）
- `README.md` 架构章节（+86 行，含两张 Mermaid 图）

**验收**：无业务代码变更，`go test ./...` 全部通过。

---

### PR #12 — Phase 1：全量内存排行榜 + Top1000 视图

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/12  
标题：实现 Phase 1：全量内存排行榜 + Top1000 视图

#### 关键 Commit

```
实现 Phase 1：全量内存排行榜 + Top1000 视图
修复：Page 分页逻辑与内存 JoinedAt 字段完整性
代码审核修复：SPEC 冲突清理、findPlayerPage 下标修正、/join Get 对齐
```

**核心实现**：
- 新增 `internal/memrank` 模块：封装全量内存排行榜逻辑
- 启动时从 SQLite `ListAll` 加载全量玩家到内存
- 维护有序 `ranked` 列表（按竞赛名次排序）与 **Top1000 专用视图**
- 计算门槛分（第 1000 名 score；不足 1000 人时为 `-1`）
- 线程安全：所有公开方法持有 `RWMutex` 读锁或写锁

**写入路径**（Phase 1）：
- `/join`、`/refresh`、`/admin/seed` 仍**同步 Upsert SQLite**
- 成功后立即调用 `leaderboard.Upsert(p)` 更新内存 + Top1000
- 行为与 v1 一致：冷却、错误文案、302、seed 回环检查等
- 无 WAL 文件（Phase 2 才引入双路径写入）

**读取路径**（性能优化）：
- `GET /`、`/search`、`/me` 从内存读取，**稳态读不再每次 `ListAll` + `SortAndNumber`**
- 分页：名次 ≤1000 走 Top1000 视图；之外走全量有序结构
- 查找：`leaderboard.Get(onlineID)` 直接返回 `RankedPlayer`，无需访问 SQLite

**性能提升**：
- 读路径：无需每次全量 SQL 查询与排序（从 O(N log N) 降为 O(1) Top1000 切片或 O(log N) 全量定位）
- 写路径：仍为 v1 水平（同步 SQLite），适合真实 PSN 低频入榜

**测试覆盖**：
- memrank 单测：加载、upsert、分页、Top1000 切片、竞赛名次（完全同分跳号）
- HTTP 集成测试：增加 `ReloadFromStore()` 辅助方法

**文档更新**：
- SPEC §10：新增 §10.1 标注 Phase 1 已落地；§10.2 回顾 v1 基线
- SPEC §13.8：标记 Phase 1 完成，列出实现模块
- README：说明当前架构状态与 `internal/memrank` 模块

---

### PR #13 — Phase 2：WAL 分片与封段刷盘

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/13  
标题：实现 Phase 2：WAL 分片与封段刷盘

#### 关键 Commit

```
实现 Phase 2：WAL 分片与封段刷盘
修复必改项：删除二进制、优化刷盘、内存去重
修复：Phase2 测试适配异步 WAL 写入
```

**核心实现**：
1. **去除 /admin/seed count 上限**：移除 `maxSeedCount=1000` 限制；仍须正整数，仍仅回环地址
2. **WAL 模块**（`internal/wal`）：
   - 10 个 hash 分片文件：`data/wal/shard-N.log`
   - 封段协议：每 100ms 触发封段 → rename `shard-N.log` 为 `sealed-<timestamp>` → 创建新空 `shard-N.log` → 读取 sealed 并去重（保留最新）→ 批量 Upsert SQLite + 更新内存榜单 → 成功后删除 sealed
   - 启动时重放未处理的 sealed 段
3. **双路径写入**：
   - **热路径**（`/join` `/refresh`）：同步 SQLite + 内存（保持 Phase 1 行为）
   - **冷路径**（`/admin/seed`）：追加到 WAL，入队即返回 `{"ok":true,"enqueued":N,"failed":M}`
4. **配置参数**：
   - `WAL_DIR`：WAL 目录路径，默认 `./data/wal/`
   - `WAL_SEAL_INTERVAL_MS`：封段间隔，默认 100ms

**验证**：
- 大批量灌数测试：`curl -X POST "http://127.0.0.1:8080/admin/seed?count=10000"`
- 验证 WAL 文件：`ls data/wal/` 显示 `shard-0.log` ~ `shard-9.log`
- 验证数据可见：排行榜显示已入榜玩家

**性能目标**：
- 入队吞吐：支持 10000+ 单次灌数
- 可见延迟：约 100ms-1s（封段间隔）
- 写入语义：`/admin/seed` 返回即入队成功，无需等待刷盘

**测试覆盖**：
- WAL 模块完整单元测试：append、分片分布、封段刷盘、去重、重放
- 更新 HTTP 测试适配异步语义

**文档更新**：
- SPEC 与 README 更新相关文案，反映 WAL 行为与可见延迟

---

### PR #14 — 修复 seed 卡住 + 硬顶100万 + pprof + slog

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/14  
标题：修复 seed 卡住 + 硬顶100万 + pprof + slog（SPEC 先行）

#### 关键 Commit

```
文档：SPEC 增补硬顶100万、pprof监控、slog日志规范、WAL锁优化说明
实现：seed count 硬顶 1000000（100万），超限返回 400 错误
实现：添加 pprof 监控支持，挂载到 /debug/pprof/ 路径
实现：全仓库日志迁移至 log/slog，移除所有 log.Printf
修复：WAL 封段 flush 锁优化，rename+创建新文件在锁内，读取+刷盘+更新在锁外
文档：README 更新硬顶100万说明、pprof用法、slog日志、WAL锁优化效果
修复：pprof 路由显式挂载到 chi + 测试优化 + SPEC 清理
```

**核心修复与实现**：

1. **WAL 锁优化**（解决 seed 卡住）：
   - **问题**：封段 flush 时长时间持锁，阻塞新写入，导致 seed count=1万时卡数秒
   - **修复**：将 `sealAndFlush` 拆分为 `sealShard`（持锁）和后续处理（锁外）
   - **持锁范围最小化**：仅 rename + 创建新文件在锁内（文件系统操作，毫秒级）
   - **锁外慢速操作**：读 sealed 段、JSON 解析、去重、批量 DB upsert、内存更新
   - **效果**：seed count=1万也能秒级返回 `enqueued` 状态

2. **Seed 硬顶 1,000,000**：
   - 单次请求最多 100 万条
   - 超限返回 400：`{"ok":false,"error":"count exceeds maximum limit of 1000000"}`
   - 边界测试：`count=1000001` 拒绝，小 count 验证通过路径

3. **pprof 监控**：
   - **显式挂载到 chi router**（不依赖 `http.DefaultServeMux`）
   - 访问路径：`http://127.0.0.1:8080/debug/pprof/`
   - 注册所有标准 pprof 端点：`/`, `cmdline`, `profile`, `symbol`, `trace`, `heap`, `allocs`, `block`, `goroutine`, `mutex`, `threadcreate`
   - 用法示例：`curl http://127.0.0.1:8080/debug/pprof/profile?seconds=30 > cpu.prof`
   - 安全：仅本机访问（与主服务共用 `LISTEN_ADDR`）
   - 测试覆盖：`TestPprofAccessible` 验证端点可达

4. **日志迁移 slog**：
   - 全仓库统一使用 `log/slog`（Go 1.21+ 标准库）
   - 结构化字段：`slog.String()`, `slog.Int()`, `slog.Duration()` 等
   - 关键事件：启动、WAL 封段、PSN API 调用、错误
   - 安全保障：禁止打印 `PSN_NPSSO`、token 等敏感凭证

**测试优化**：
- `TestAdminSeedExceedsMax` 拆分为两个测试，不真跑 count=1000000（避免慢/爆内存）
- `TestAdminSeedAtMaxLimit` 仅验证边界检查逻辑和常量值
- `TestPprofAccessible` 新增，验证 pprof 端点可达
- 所有测试快速通过（http 测试仅 0.466s）

**文档更新**：
- SPEC §4.2：`/admin/seed` 硬顶、WAL 语义、可见延迟
- SPEC §7.5：新增可观测性节（pprof + slog）
- SPEC §13.3.3：WAL 锁优化详细说明，清理重复段落
- README：`/admin/seed` 硬顶说明、pprof 用法示例、日志规范

---

### PR #15 — 批量内存更新：优化 WAL flush 性能

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/15  
标题：批量内存更新：优化 WAL flush 性能

#### 关键 Commit

```
feat: 批量内存更新优化 WAL flush 性能
```

**核心实现**：

1. **`internal/memrank` 新增 `UpsertBatch` 方法**：
   - 批量写入玩家到内存 map，**只 rebuild 一次**
   - 避免每个玩家都触发全量排序（O(N log N)）
   - 适用于 WAL flush 等批量更新场景

2. **`internal/http` 新增 `UpsertMemoryBatch` 方法**：
   - 封装 `leaderboard.UpsertBatch` 供外部调用
   - 保持与现有 `UpsertMemory` 单个更新方法一致的接口风格

3. **`cmd/server` 优化 WAL flush 流程**：
   - **ReplaySealed**: 先循环 `store.Upsert` 写 SQLite，再一次 `UpsertMemoryBatch` 更新内存
   - **StartSealing**: 同样改用批量更新，不再每玩家调用 `UpsertMemory`

4. **单测覆盖**：
   - `TestLeaderboard_UpsertBatch`: 正常插入、更新、排名验证
   - `TestLeaderboard_UpsertBatch_Empty`: 空批次边界情况
   - `TestLeaderboard_UpsertBatch_Large`: 1000 人大批量场景，验证门槛分计算

**性能收益**：
- **WAL flush 批量场景**：N 个玩家只触发 1 次排序，而非 N 次
- **示例**：count=10000 压测时，每次封段 flush 从 N 次 O(N log N) 优化到 1 次 O(N log N)

**测试**：✅ `go test ./...` 全部通过

---

### PR #16 — 性能优化：排序算法优化 A+B

https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/16  
标题：性能优化：排序算法优化 A+B

#### 关键 Commit

```
性能优化：排序算法优化 A+B
```

**背景**：pprof 显示 `internal/rank.SortAndNumber` 的插入排序占 ~77% CPU。本 PR 实现两项优化。

**优化 A：标准库排序替代插入排序**：
- `internal/rank`: 将 `SortAndNumber` 中的 O(N²) 插入排序改为 `slices.SortFunc`
- 新增 `comparePlayers` 函数实现 cmp 比较逻辑（返回 -1/0/1）
- 保留 `less` 函数用于内部兼容性
- 排序键与 SPEC 一致：score/platinum/gold/silver/bronze 降序，online_id 升序（大小写不敏感）
- 新增大数据量测试 `TestSortAndNumber_LargeScale`（1k/10k/50k 玩家）

**优化 B：WAL 批量更新使用有序合并**：
- `internal/memrank`: 增强 `UpsertBatch` 方法，从简单 rebuild 改为有序合并
- 实现有序合并算法：
  1. 对批次每个 online_id：若已在 `ranked` 中，先移除（使用 `indexByID` 快速定位）
  2. 将批次转为 rank.Player，用标准库排序（优化 A）
  3. 与现有有序 `ranked` 做二路归并
  4. 线性扫描赋竞赛名次
  5. 更新 top1000、threshold、players map、index map
- 复杂度从 O(M * N log N) 优化为 O(M log M + N)（批量 M，总量 N）
- 新增 `indexByID map[string]int` 维护玩家下标索引，实现 O(1) 查找
- 优化 `Get` 和 `IndexOf` 方法使用 indexByID，从 O(N) 降为 O(1)

**集成说明**：
- 复用已有的 `UpsertMemoryBatch` 接口（不引入新命名）
- cmd/server 已使用 `UpsertMemoryBatch`，调用路径保持不变
- Load / 单条 Upsert 仍可走标准库排序（单条低频 OK）

**测试验证**：
- 新增 `TestLeaderboard_UpsertBatch` 测试批量更新和排序正确性
- 新增 `TestLeaderboard_UpsertBatch_LargeScale` 测试 10k 玩家批量更新 1k 条
- 所有 memrank 测试通过，包括竞赛名次、分页、Top1000 等
- ✅ `go test ./...` 全部通过

**成功标准**：
- ✅ 1 万～10 万 rebuild/flush 不再卡在插入排序
- ✅ 测试绿
- ✅ 文档更新（README / SPEC Phase 1+ 章节）

---

## 验证状态

```bash
go test ./...    # 全部包通过
go build ./...   # 构建成功
```

### 功能覆盖（当前主分支）

**v1 基线（PR #1–#10）**：
- PRD FR-1 至 FR-21（US-005 为 `/admin/seed`，不再是 fixture）
- 七个用户故事 US-001 至 US-007
- 头像：https 保留；Sony CDN http 升级；加载失败首字母回退
- 冷却：`players.synced_at`，重启后仍生效
- 刷新：`POST /refresh`，页码不丢失

**高吞吐架构（PR #11–#16）**：
- Phase 1：全量内存 + Top1000 视图，读性能提升
- Phase 2：WAL 分片 + 封段刷盘，支持大批量灌数（硬顶 100 万）
- pprof 监控：`/debug/pprof/` 路径，本机访问
- slog 日志：全仓库统一结构化日志
- WAL 锁优化：封段 flush 秒级返回，不卡写入
- 批量内存更新：`UpsertMemoryBatch` 避免逐条 rebuild
- 排序优化 A：标准库排序 `slices.SortFunc`，O(N log N)
- 排序优化 B：有序合并 O(M log M + N)，O(1) 查找

---

## 分支与 PR

- **main**：已合并 PR #1–#16，与 `origin/main` 一致
- 功能分支（`cursor/...`）可保留作历史对照，以 main 为准

| PR | 链接 |
|----|------|
| #1 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/1 |
| #2 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/2 |
| #3 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/3 |
| #4 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/4 |
| #5 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/5 |
| #6 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/6 |
| #7 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/7 |
| #8 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/8 |
| #9 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/9 |
| #10 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/10 |
| #11 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/11 |
| #12 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/12 |
| #13 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/13 |
| #14 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/14 |
| #15 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/15 |
| #16 | https://github.com/cuteLittleDevil/ps-trophy-ranking/pull/16 |

---

*更新时间: 2026-09-11（对齐 main `572781a`，覆盖 PR #1–#16）*
